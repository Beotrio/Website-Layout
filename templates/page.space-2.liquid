{%- assign bodyClasses = '' -%}
{%- section 'announcement-bar' -%}

<!-- Use the same background as home page -->
{%- section 'default-page-background' -%}

<div class="site-container" style="width: 100%; margin: 0; height: max-content;">
  <!-- Optional: Add logo-container if needed -->
  <div class="content-container" style="padding: 0 20px; min-height: 100vh;">
    {% section 'interactive-environments-1' %}
  </div>

  <!-- Footer (if needed) -->
  <div style="margin-top: auto; height: auto; width: 100%; position: relative; bottom: 0;">
    {% if settings.show_footer %}{%- section 'other-footer' -%}{% endif %}
  </div>
</div>

<!-- Load scripts consistently -->
<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
<script src="{{ 'plain-jane.js' | asset_url }}" defer></script>
